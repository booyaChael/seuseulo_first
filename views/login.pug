doctype html 
html(lang="ko")
    head        
        title #{pageTitle} | Wetube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header 
            nav 
                ul
                    li 
                        a(href="/") 홈
                    li 
                        a(href="/login") 로그인
        form
            input(name="user_id", placeholder="아이디", required, type="text")
            input(name="user_password", placeholder="비밀번호", required, type="password")
            input(type="submit", value="로그인")